diff --git a/main/application.cc b/main/application.cc
index d54ac6b..33d39b2 100644
--- a/main/application.cc
+++ b/main/application.cc
@@ -327,11 +327,15 @@ void Application::ActivationTask() {
     // Check for new assets version
     CheckAssetsVersion();
 
+#if CONFIG_TAB5_XIAOZHI_OFFLINE_ONLY
+    ESP_LOGW(TAG, "Tab5 xiaozhi offline mode: skip OTA and protocol startup");
+#else
     // Check for new firmware version
     CheckNewVersion();
 
     // Initialize the protocol
     InitializeProtocol();
+#endif
 
     // Signal completion to main loop
     xEventGroupSetBits(event_group_, MAIN_EVENT_ACTIVATION_DONE);
@@ -1128,4 +1132,3 @@ void Application::ResetProtocol() {
         protocol_.reset();
     });
 }
-
diff --git a/main/ota.cc b/main/ota.cc
index bcc0dc5..0b59cad 100644
--- a/main/ota.cc
+++ b/main/ota.cc
@@ -18,6 +18,7 @@
 #endif
 
 #include <cstring>
+#include <cctype>
 #include <vector>
 #include <sstream>
 #include <algorithm>
@@ -397,9 +398,26 @@ std::vector<int> Ota::ParseVersion(const std::string& version) {
     std::string segment;
     
     while (std::getline(ss, segment, '.')) {
-        versionNumbers.push_back(std::stoi(segment));
+        // Be tolerant of non-numeric version strings (e.g. git-describe
+        // versions like "V0.4-4-g3ed0fde-dirty"). std::stoi would throw on a
+        // non-digit segment and, being uncaught, abort the whole device.
+        // Parse the leading integer if present, otherwise treat as 0.
+        int value = 0;
+        try {
+            size_t pos = 0;
+            // skip a leading non-digit (e.g. the 'V' in "V0")
+            while (pos < segment.size() && !std::isdigit((unsigned char)segment[pos])) {
+                ++pos;
+            }
+            if (pos < segment.size()) {
+                value = std::stoi(segment.substr(pos));
+            }
+        } catch (...) {
+            value = 0;
+        }
+        versionNumbers.push_back(value);
     }
-    
+
     return versionNumbers;
 }
 
